/*
$(document).ready(function () {
    // Ocultar botones con id "delete_button" en todas las DetailViews
    $('#delete_button').hide();

    // Opcional: Si prefieres deshabilitarlos en lugar de ocultarlos
    // $('#delete_button').prop('disabled', true).css('pointer-events', 'none');
});
*/